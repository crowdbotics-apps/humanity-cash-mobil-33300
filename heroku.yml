build:
  docker:
    web: ./backend/Dockerfile
  config:
    DEBUG_COLLECTSTATIC: 1
    SECRET_KEY: build
release:
  image: web
  command:
    - python3 manage.py migrate && python3 manage.py cities_light && python3 manage.py clean_blockchain_data